# Frontend (Vite + React)

Quick start:

Install deps:

```bash
cd frontend
npm install
```

Run dev server:

```bash
npm run dev
```

The app expects an API endpoint at `/api/books` returning JSON array of books with `id`, `title`, and `author`.
